/**
 * Legacy import boundary retained for repository history.
 *
 * Design Architecture 2.0 deliberately removed simulated competition data
 * from production code. Product routes must expose an unavailable state when
 * a verified source is not connected instead of importing local race rows.
 */
export {};
